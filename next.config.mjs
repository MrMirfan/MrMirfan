/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: 'out',
    basePath: '/MrMirfan',
    images: {
        unoptimized: true,
      },
};

export default nextConfig;
